# Omacole Tmux Configuration
# Keeps all Tmux defaults (prefix is Ctrl+b)

# Terminal settings
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

# Enable mouse support (scroll, click, resize panes)
set -g mouse on

# Start windows and panes at 1 instead of 0
set -g base-index 1
setw -g pane-base-index 1

# Renumber windows when one is closed
set -g renumber-windows on

# Increase scrollback buffer
set -g history-limit 50000

# Reduce escape time (important for Neovim)
set -sg escape-time 10

# Focus events (needed for Neovim autoread)
set -g focus-events on

# Source theme
source-file ~/.config/tmux/theme.conf

# --- Optional hjkl pane navigation (uncomment to enable) ---
# Note: enabling 'l' overrides the default 'last-window' binding
# bind h select-pane -L
# bind j select-pane -D
# bind k select-pane -U
# bind l select-pane -R
# bind -r H resize-pane -L 5
# bind -r J resize-pane -D 5
# bind -r K resize-pane -U 5
# bind -r L resize-pane -R 5
